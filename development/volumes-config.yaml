alb:
  addr: 0.0.0.0
  ports:
    http: 3000
  reconciliation: /reconcile

services:
  # echo:
  #   image: echo
  #   tag: double
  #   replicas: 1
  #   routes:
  #     - host: localhost:3000
  #       port: 8080

  volumes:
    image: volumes
    tag: latest
    replicas: 1
    routes:
      - host: localhost:3000
        port: 8080
    volumes:
      data:
        source:
          location: filesystem
          path: /Users/personal/Documents/code/f2/development/volumes-configuration.json
        target: /data/configuration.json
